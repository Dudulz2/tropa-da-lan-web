# Tropa da Lan V7.0.4 — Database Fix

1. No Supabase SQL Editor, execute `DB_REPAIR_V7.sql`.
2. O resultado final deve mostrar `Tropa V7 reparada` e todos os campos `*_ok` como `true`.
3. Suba `app.js`, `index.html` e `sw.js` desta versão para o GitHub Pages.
4. Faça Ctrl+Shift+R.

Não execute `supabase_v4.sql` novamente se sua V6 já funcionava.
